fragment Post on Post {
	id
	title
	url
	author {
		name
		profilePicture
	}
	coverImage {
		url
	}
	publishedAt
	readTimeInMinutes
	slug
	brief
	tags {
		name
		slug
	}
	comments(first: 0) {
		totalDocuments
	}
}
